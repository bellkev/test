test:
  override:
      - if [ $CIRCLE_NODE_INDEX == "0" ] ; then echo "hello" ; fi
        parallel: true
      - if [ $CIRCLE_NODE_INDEX > "0" ] ; then echo "goodbye" ; fi
        parallel: true
      
